<?xml version="1.0" encoding="utf-8"?>
<mx:Panel xmlns:mx="http://www.adobe.com/2006/mxml" 
	toolTip="Click to drag, Double click to set as root"
	xmlns:polar="birdeye.vis.coords.*" borderThickness="10"
	xmlns:elements="birdeye.vis.elements.geometry.*" title="{_visualObjectID}"
	xmlns:scales="birdeye.vis.scales.*" headerHeight="10" fontSize="7" textAlign="center"
	implements="birdeye.vis.interfaces.data.IVisualDataID"
	creationComplete="createDT()" buttonMode="true"
	horizontalAlign="center" verticalAlign="middle" >
	
<mx:Script>
		<![CDATA[
			import birdeye.vis.interfaces.data.IVisualDataID;
			import mx.collections.ArrayCollection;
			import birdeye.vis.elements.util.EffectsUtil;
			
			[Bindable]
			public var dt:ArrayCollection;;

			[Bindable]
			public var dt1:ArrayCollection;;

			[Bindable]
			private var _visualObjectID:String;
			/** Identify this data item among the element ones.*/
			public function set visualObjectID(val:String):void
			{
				_visualObjectID = val;
			}
			public function get visualObjectID():String
			{
				return _visualObjectID;
			}
	

			private var subject:Array;
			
			private var regions:Array = ["R1", "R2", "R3", "R4"];
			private function createDT():void
			{
				var ac:ArrayCollection = new ArrayCollection();
				for (var i:Number = 0; i<regions.length; i++)
				{
					ac.addItem({
						region: regions[i],
						GDP: Math.round(Math.random() * 1000),
						Area: Math.round(Math.random() * 1000),
						Density: Math.round(Math.random() * 1000),
						Population: Math.round(Math.random() * 1000)
					});
				}
				dt = ac;

				var ac1:ArrayCollection = new ArrayCollection();
				for (i = 1; i<40; i++)
					ac1.addItem({
						R1: Math.round(Math.random() * i*10* 1000)
					});

				dt = ac;
				dt1 = ac1;
			}

			private function dtFunc(currDatatip:Object, dataFields:Array):String
			{
				var tt:String = currDatatip[dataFields["dim1"]];
				return  tt;
			}
		]]>
	</mx:Script>

	<polar:Polar dataProvider="{dt}" width="100%" height="100%" dataTipFunction="dtFunc" buttonMode="false">
		<polar:scales>
			<scales:Numeric id="l" scaleType="constant" />
			<scales:PercentAngle id="a" size="360"/>
		</polar:scales>
		<polar:elements>
			<elements:WedgeElement mouseOverFunction="EffectsUtil.setGlowEffect" 
				mouseOutFunction="EffectsUtil.removeEffects" 
				scale1="{a}" scale2="{l}" alpha=".6"
				labelField="region" 
				radiusLabelOffset="5" 
				colorsStart="[0xE8480A,0xFF940D,0xFF0000,0xE80CDE]" 
				colorsStop="[0xE8800A,0xFFEB0D,0xFF2300,0x8E0CE8]" 
				extendMouseEvents="true" dim1="GDP" />
		</polar:elements>
	</polar:Polar>

</mx:Panel>
